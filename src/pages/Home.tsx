import { Container } from '@mui/material';
import React from 'react'
import { Colors } from '../design/theme';

const Home: React.FC = () => {
  return (
    <Container sx={{ minHeight: "100%", padding:0, margin:0}}>Home</Container>
  )
}

export default Home;